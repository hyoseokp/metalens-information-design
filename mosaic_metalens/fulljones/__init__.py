"""Vendored full-Jones forward engine (production engine2 subset, forward-scoring closure).

These modules reproduce the manuscript numbers. Ported verbatim from the
production engine; only the forward-scoring path is included (no optimizer).
"""
